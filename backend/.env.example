# Server Port
PORT=5000

# Environment Mode (development / production)
NODE_ENV=development

# MongoDB Connection String (Leave empty or omit to use automatic in-memory MongoDB fallback)
MONGODB_URI=

# JWT Secret Key for Bearer Authentication
JWT_SECRET=veloop_super_secret_jwt_key_2026

# Timezone for Server-Authoritative Calendar Date Calculations
TIMEZONE=Asia/Kolkata
